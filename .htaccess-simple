# Simple .htaccess without proxy modules
# Try this first - basic redirect approach

RewriteEngine On

# Test if basic rewrite works
RewriteRule ^test$ - [R=200,L]

# If proxy modules aren't available, we can't do much with .htaccess
# This file is mainly for testing if mod_rewrite works
# Hugo Contact Form - Production Environment Configuration Example
# Copy this file to .env.production and configure with your production settings
# NEVER commit the actual .env.production file with real secrets!

# SMTP Configuration
SMTP_HOST=your-smtp-server.com
SMTP_PORT=587
SMTP_USERNAME=your-smtp-username
SMTP_PASSWORD=your-smtp-password-stored-in-github-secrets
SENDER_EMAIL=system@yourdomain.com
RECIPIENT_EMAIL=contact@yourdomain.com

# CORS Configuration (Multiple Domains - Comma Separated)
# Add all domains where your Hugo site is hosted
CORS_ALLOW_ORIGINS=https://yourdomain.com,https://www.yourdomain.com,https://staging.yourdomain.com

# Security Token (Generate a secure 32+ character secret)
# Use a strong random string - store in GitHub Secrets
TOKEN_SECRET=CHANGE-THIS-TO-A-SECURE-32-CHAR-SECRET-FROM-GITHUB-SECRETS

# Server Configuration
PORT=8080

# Optional: Rate Limiting (requests per minute)
RATE_LIMIT=10

# Optional: Custom Success Redirect
# DEFAULT_REDIRECT=https://yourdomain.com/thank-you

# Optional: Honeypot Field Names (comma-separated)
# HONEYPOT_FIELDS=_gotcha,nickname,url

# Optional: Required Fields (comma-separated)
# REQUIRED_FIELDS=name,email,message

# Optional: Email Subject Template
# EMAIL_SUBJECT=New Contact Form Submission from {{.Name}}

# Optional: Debug Mode (never use in production)
# DEBUG=false
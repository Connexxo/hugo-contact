# Hugo Contact Form - Production Environment Template
# Copy this file to .env.production and fill in your actual values

# ============================================
# HTTPS Configuration
# ============================================

# Enable HTTPS mode (set to "true" to use HTTPS)
USE_HTTPS=true

# Port to listen on (443 for standard HTTPS, requires sudo)
PORT=443

# SSL Certificate paths (absolute paths on your server)
# Example: /etc/letsencrypt/live/contact.connexxo.com/fullchain.pem
SSL_CERT_PATH=/path/to/your/certificate.crt
SSL_KEY_PATH=/path/to/your/private.key

# ============================================
# SMTP Configuration
# ============================================

# SMTP server details
SMTP_HOST=your-smtp-server.com
SMTP_PORT=587
SMTP_USERNAME=your-smtp-username
SMTP_PASSWORD=your-smtp-password

# Email addresses
SENDER_EMAIL=noreply@yourdomain.com
RECIPIENT_EMAIL=contact@yourdomain.com

# ============================================
# CORS Configuration
# ============================================

# Comma-separated list of allowed origins
# Example: https://yourdomain.com,https://www.yourdomain.com
CORS_ALLOW_ORIGINS=https://connexxo.com,https://www.connexxo.com

# ============================================
# Security Configuration
# ============================================

# Secret token for form validation (minimum 16 characters)
# Generate a secure random string, e.g.: openssl rand -base64 32
TOKEN_SECRET=your-secret-token-here-min-16-chars

# ============================================
# Optional Configuration
# ============================================

# External URL for status checks (optional)
# EXTERNAL_URL=https://contact.connexxo.com

# Log level (optional, defaults to info)
# LOG_LEVEL=debug

# ============================================
# Notes
# ============================================
# 
# 1. SSL Certificates:
#    - For Let's Encrypt: Usually in /etc/letsencrypt/live/domain/
#    - For purchased certs: Often in /etc/ssl/certs/ and /etc/ssl/private/
#    - Make sure the user running the server can read the certificate files
#
# 2. SMTP Settings:
#    - Port 587: TLS/STARTTLS (most common)
#    - Port 465: SSL
#    - Port 25: Unencrypted (not recommended)
#
# 3. Security:
#    - Never commit the actual .env.production file to git
#    - Keep TOKEN_SECRET truly random and secret
#    - Regularly rotate the TOKEN_SECRET for security
#
# 4. Permissions:
#    - Port 443 requires root/sudo privileges
#    - Consider using port 8443 to avoid needing sudo
#    - Make sure this file is readable only by the server user